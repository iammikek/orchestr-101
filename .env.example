# Application
APP_NAME="Orchestr 101"
APP_ENV=local
APP_DEBUG=true
APP_PORT=3000
APP_HOST=localhost

# Database (SQLite)
DB_CONNECTION=sqlite
DB_DATABASE=database/database.sqlite

# API key for protected endpoints (e.g. DELETE /items/:id)
# Send as header: X-API-Key
API_KEY=dev-key-123
